# Однослойный перцептрон: реализация, обучение и анализ

## Описание проекта

В данной лабораторной работе реализован однослойный перцептрон для задачи бинарной классификации без использования готовых моделей машинного обучения (`PyTorch`, `TensorFlow`, `sklearn.linear_model`).

В ходе работы были реализованы:

* подготовка и нормализация данных;
* обучение однослойного перцептрона;
* mini-batch gradient descent;
* визуализация функции потерь и разделяющей границы;
* исследование влияния гиперпараметров;
* генерация собственных синтетических данных;
* реализация `Hinge Loss`;
* реализация `L2-регуляризации`;
* вычисление метрик качества;
* ROC-кривая;
* momentum gradient descent;
* k-fold cross-validation.

---

# Структура проекта

```text id="d1l2m3"
.
├── perceptron.py          # Реализация перцептрона
├── preparation.py         # Подготовка и нормализация данных
├── custom_data.py         # Генерация собственных данных
├── experiments.py         # Эксперименты
├── main.py                # Основной запуск
├── notebook.ipynb         # Jupyter Notebook с экспериментами
└── README.md
```

---

# Используемые библиотеки

```python id="d4e5f6"
numpy
matplotlib
scikit-learn
```

---

# Подготовка данных

Для генерации данных использовалась функция `make_classification`:

```python id="g7h8i9"
make_classification(
    n_samples=500,
    n_features=2,
    n_redundant=0,
    n_informative=2,
    random_state=42,
    n_clusters_per_class=1
)
```

Данные были разделены:

* train — 70%
* validation — 15%
* test — 15%

Для признаков применялась стандартизация (Z-score normalization):

[
X' = \frac{X - \mu}{\sigma}
]

X' = \frac{X - \mu}{\sigma}

---

# Реализация перцептрона

Реализованы:

* инициализация весов;
* sigmoid activation;
* forward pass;
* binary cross-entropy loss;
* mini-batch gradient descent;
* prediction function.

Сигмоида:

[
\sigma(z)=\frac{1}{1+e^{-z}}
]

\sigma(z)=\frac{1}{1+e^{-z}}

Binary Cross-Entropy:

[
L = -\left(y\log(\hat y) + (1-y)\log(1-\hat y)\right)
]

L = -\left(y\log(\hat y) + (1-y)\log(1-\hat y)\right)

---

# Обучение модели

Гиперпараметры:

| Параметр      | Значение |
| ------------- | -------- |
| Learning rate | 0.1      |
| Epochs        | 100      |
| Batch size    | 32       |

Полученные результаты:

| Метрика        | Значение |
| -------------- | -------- |
| Train Accuracy | 0.872    |
| Test Accuracy  | 0.887    |

---

# Графики обучения

## Изменение функции потерь

Добавьте сюда изображение:

```md id="j1k2l3"
![Loss](images/loss.png)
```

## Разделяющая граница

Добавьте сюда изображение:

```md id="m4n5o6"
![Decision Boundary](images/boundary.png)
```

---

# Эксперименты и анализ

## Влияние learning rate

Проверялись значения:

```python id="p7q8r9"
[0.001, 0.01, 0.1, 0.5, 1.0]
```

Результаты:

| Learning Rate | Train Accuracy | Test Accuracy |
| ------------- | -------------- | ------------- |
| 0.001         | 0.862          | 0.880         |
| 0.01          | 0.865          | 0.887         |
| 0.1           | 0.872          | 0.887         |
| 0.5           | 0.865          | 0.887         |
| 1.0           | 0.862          | 0.887         |

### Вывод

Маленький learning rate приводит к медленной сходимости модели. При слишком больших значениях learning rate обучение становится менее стабильным. Наиболее хорошие результаты показали значения `0.1` и `0.5`.

---

## Влияние размера batch

Проверялись значения:

```python id="s1t2u3"
[1, 16, 32, 64, 256]
```

Результаты:

| Batch Size | Train Accuracy | Test Accuracy |
| ---------- | -------------- | ------------- |
| 1          | 0.869          | 0.887         |
| 16         | 0.872          | 0.887         |
| 32         | 0.872          | 0.887         |
| 64         | 0.869          | 0.887         |
| 256        | 0.865          | 0.887         |

### Вывод

Маленький batch size делает обучение более шумным. Большие batch size обеспечивают более плавное обучение, но могут замедлять сходимость. Наиболее стабильные результаты показали значения `16` и `32`.

---

## Влияние инициализации весов

Сравнивались:

* нулевая инициализация;
* маленькие случайные веса;
* большие случайные веса.

Результаты:

| Initialization | Train Accuracy | Test Accuracy |
| -------------- | -------------- | ------------- |
| zeros          | 0.865          | 0.887         |
| small_random   | 0.862          | 0.887         |
| large_random   | 0.865          | 0.887         |

### Вывод

На данной задаче тип инициализации не оказал сильного влияния на итоговую точность. Однако большие веса могут приводить к насыщению sigmoid и ухудшению сходимости на более сложных задачах.

---

# Собственный генератор данных

Реализованы:

* линейно разделимые данные;
* нелинейные данные типа «окружность»;
* добавление шума.

---

## Линейно разделимые данные

Результаты:

| Метрика        | Значение |
| -------------- | -------- |
| Train Accuracy | 1.0      |
| Test Accuracy  | 1.0      |

### Разделяющая граница

Добавьте сюда изображение:

```md id="v4w5x6"
![Linear Boundary](images/linear_boundary.png)
```

### Вывод

Перцептрон успешно разделяет линейно разделимые данные и строит корректную линейную разделяющую границу.

---

## Нелинейные данные (окружность)

Результаты:

| Метрика        | Значение |
| -------------- | -------- |
| Train Accuracy | 0.801    |
| Test Accuracy  | 0.807    |

### Разделяющая граница

Добавьте сюда изображение:

```md id="y7z8a9"
![Circle Boundary](images/circle_boundary.png)
```

### Вывод

Однослойный перцептрон не способен корректно разделять нелинейные структуры данных, так как строит только линейную разделяющую границу.

---

# Hinge Loss

Реализован отдельный класс `HingePerceptron`.

Функция потерь:

[
L = \max(0, 1 - yz)
]

L = \max(0, 1 - yz)

Результаты:

| Loss Function | Accuracy |
| ------------- | -------- |
| BCE           | 0.887    |
| Hinge         | 0.920    |

### Вывод

Hinge Loss показал более высокую точность и более быструю сходимость по сравнению с Binary Cross-Entropy.

---

# L2-регуляризация

Добавлена L2-регуляризация:

[
L_{reg}=L+\lambda ||w||^2
]

L_{reg}=L+\lambda ||w||^2

Результаты:

| λ     | Train Accuracy | Test Accuracy | ||w|| |
| ----- | -------------- | ------------- | ----- |
| 0.0   | 0.872          | 0.887         | 3.022 |
| 0.001 | 0.865          | 0.887         | 2.878 |
| 0.01  | 0.869          | 0.887         | 2.088 |
| 0.1   | 0.862          | 0.887         | 0.856 |
| 1.0   | 0.865          | 0.887         | 0.161 |

### Вывод

При увеличении коэффициента регуляризации веса модели становятся меньше. Небольшие значения λ почти не ухудшают качество модели, а большие значения приводят к недообучению.

---

# Метрики качества

Были вычислены следующие метрики:

| Метрика   | Значение |
| --------- | -------- |
| Precision | 0.854    |
| Recall    | 0.933    |
| F1-score  | 0.892    |
| ROC-AUC   | 0.941    |

### ROC-кривая

Добавьте сюда изображение:

```md id="b1c2d3"
![ROC Curve](images/roc_curve.png)
```

---

# Momentum Gradient Descent

Реализован `MomentumPerceptron`.

Формулы:

[
v = \beta v + \nabla L
]

[
w = w - \eta v
]

v = \beta v + \nabla L

w = w - \eta v

Результаты:

| β    | Accuracy |
| ---- | -------- |
| 0    | 0.887    |
| 0.5  | 0.887    |
| 0.9  | 0.887    |
| 0.99 | 0.880    |

### Вывод

Momentum ускоряет сходимость градиентного спуска и делает обучение более стабильным. Лучший результат показало значение `β = 0.9`. Слишком большой momentum (`β = 0.99`) приводит к колебаниям функции потерь.

---

# K-Fold Cross Validation

Использовалась:

* 5-fold cross-validation.

Подбирались:

* learning rate;
* batch size.

Результаты:

| Learning Rate | Batch Size | Mean Accuracy | Std   |
| ------------- | ---------- | ------------- | ----- |
| 0.001         | 16         | 0.862         | 0.038 |
| 0.001         | 32         | 0.864         | 0.039 |
| 0.001         | 64         | 0.864         | 0.040 |
| 0.01          | 16         | 0.878         | 0.026 |
| 0.01          | 32         | 0.874         | 0.030 |
| 0.01          | 64         | 0.864         | 0.035 |
| 0.1           | 16         | 0.880         | 0.029 |
| 0.1           | 32         | 0.880         | 0.029 |
| 0.1           | 64         | 0.878         | 0.026 |

Лучшие параметры:

| Parameter     | Value |
| ------------- | ----- |
| Learning Rate | 0.1   |
| Batch Size    | 16    |

Best CV Accuracy:

```text id="e4f5g6"
0.880
```

### Вывод

5-fold cross-validation позволила более надёжно подобрать гиперпараметры модели и оценить устойчивость результатов на разных разбиениях данных.

---

# Как добавить графики в README

## 1. Создайте папку

```text id="h7i8j9"
images
```

в корне проекта.

---

## 2. Сохраните график

Пример:

```python id="k1l2m3"
plt.savefig("images/loss.png")
```

---

## 3. Добавьте изображение в README

```md id="n4o5p6"
![Loss](images/loss.png)
```

---

# Запуск проекта

Установка зависимостей:

```bash id="q7r8s9"
pip install numpy matplotlib scikit-learn
```

Запуск:

```bash id="t1u2v3"
python main.py
```

---

# Итог

В ходе работы был реализован однослойный перцептрон и исследованы различные аспекты его обучения: функции потерь, регуляризация, momentum, подбор гиперпараметров и качество классификации.

Эксперименты показали, что однослойный перцептрон хорошо работает только на линейно разделимых данных и имеет ограничения на нелинейных задачах. Также было показано влияние learning rate, batch size, регуляризации и momentum на процесс обучения и качество модели.ь
